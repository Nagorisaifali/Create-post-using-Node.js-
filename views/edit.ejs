<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
   

    <div class="w-full text-white h-full bg-zinc-900 p-10 flex justify-center items-center flex-col">
        <div class="nav mb-8">
            <a class="text-blue-500 " href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">Update User</h1>
        <form class="flex flex-col gap-5" action="/update/<%= user._id  %>" method="post">
            <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" placeholder="Name....." name="name" value="<%= user.name %>"> 
            <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" placeholder="email....." name="email" value="<%= user.email %>"> 
            <input class="px-5 py-3 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" placeholder="image url....." name="image" value="<%= user.image %>">
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update user"> 
        </form>
    </div>

    <script src="/javascripts/"></script>
</body>
</html>